#!Groovy

pipeline {

    agent any

    options { 
        checkoutToSubdirectory('mozart2')
    }

    stages {

        stage ('Test Docker') {
            steps {
		sh 'll'
		sh 'docker info'
		sh 'docker run hello-world'
            }
        }

    }
}
